---

- hosts: all
  become: true
  tasks:


#  - name: update repository index
 #   apt: 
  #    update_cache: yes
    #when: ansible_distribution == "Kali"
  #  when: ansible_facts["distribution"] == "Kali" 
  - name: Install the apache2 package
    apt:
      name: 
        - apache2
        - libapache2-mod-php
      state: latest
      update_cache: yes
    #when: ansible_distribution == "Kali"  deprecated in favor of below line
    when: ansible_facts["distribution"] == "Kali"

 # - name: 
  #  apt: 
   #   name: libapache2-mod-php
    #  state: latest
    #when: ansible_distribution == "Kali"
#   when: ansible_facts["distribution"] == "Kali"

#  - name: update Fedora repository index
 #   dnf: 
  #    update_cache: yes
    #when: ansible_distribution == "Fedora"
  #  when: ansible_facts["distribution"] == "Fedora"

  - name: Install the httpd package
    dnf:
      name: 
        - httpd
        - php
      state: latest
      update_cache: yes
    #when: ansible_distribution == "Fedora"
    when: ansible_facts["distribution"] == "Fedora"

#  - name: add php support for httpd
 #   dnf: 
  #    name: php
   #   state: latest
    #when: ansible_distribution == "Fedora"
    #when: ansible_facts["distribution"] == "Fedora"


